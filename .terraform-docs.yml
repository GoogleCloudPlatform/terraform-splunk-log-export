formatter: markdown table
output:
  file: README.md
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->
sort:
  enabled: true
  by: required
content: |-
  {{ .Inputs }}
  {{ .Outputs }}
settings:
  indent: 4
  escape: false
  default: false
  required: false
  type: true
